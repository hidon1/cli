rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /orders/{orderId} {
      // מערכת ניהול ההזמנות פתוחה לצפייה ולניהול ללא התחברות, לפי דרישת העסק.
      allow read, create, update, delete: if true;
    }
  }
}
